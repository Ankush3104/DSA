#include <stdio.h>
#include <conio.h>

int main(void)
{
    int a = 0, b = 0;

    printf("The value of a and b is %d\n ",a&&b);
    printf("The value of a and b is %d\n ",a||b);
    printf("The value of not (a) is %d ",!a);

    getch();
    return 0;
}
